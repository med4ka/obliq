# Pipeline & app stack. Python 3.14 -- semua paket ini sudah punya wheel cp314.
sqlalchemy>=2.0
psycopg[binary]>=3.2
python-dotenv>=1.0
streamlit>=1.40
pandas>=2.2
# Fase 1: validasi skema eksternal, HTTP fetch, migration, test
pydantic>=2.8
requests>=2.31
alembic>=1.13
pytest>=8.0
pytest-cov>=5.0  # coverage report (dipakai saat audit Sesi 19)
# Fase 1: API layer (baca dari database, bukan pipeline -- ARCHITECTURE.md 3)
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
# Fase 1: scraping tabel HTML halaman DJPPR (parser) + parsing PDF fallback
beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11
# Fase 1: jadwal fetch otomatis (APScheduler, sesuai ARCHITECTURE.md 2)
apscheduler>=3.10
# Fase 1: dashboard Streamlit (charts & skeleton placeholder)
plotly>=5.20
# Fase 4: auth (JWT httpOnly + bcrypt)
bcrypt>=4.2
pyjwt>=2.9